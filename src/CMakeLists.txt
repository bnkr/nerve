
# TODO:
#   Once we're done with the prototypes, the various programs should be done in
#   the main list file as per my normal convention.

add_subdirectory("nerve")
add_subdirectory("nerved")
add_subdirectory("gapless-proto")
add_subdirectory("socket-proto")

